I have learned about the 2 desing methods: service registry and Discovery. 

One… well, register all the microservices metadata so the configuration on big picture is easy to configure and make changes, the other one makes gives directions on where to find everything on the web, is an addition to the service registry. 